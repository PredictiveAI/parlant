import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('ParlantAdmin', () => App);
AppRegistry.runApplication('ParlantAdmin', {
  rootTag: document.getElementById('root'),
});
